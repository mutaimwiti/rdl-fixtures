# python/ambiguous

**Detection:** no `pyproject.toml` tool keys, no `Pipfile`, no `pixi.toml`

**Expected MT outcome:** Surface a user-prompt question; default to `uv` (highest-priority surviving manager)
